apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

replicas:
  - name: {{APP_NAME}}
    count: 1

configMapGenerator:
  - name: {{APP_NAME}}
    behavior: merge
    literals:
      - EXAMPLE_KEY=example-value  # TODO: replace with {{ENV}}-specific config
