apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: {{APP_NAME}}

resources:
  - statefulset.yaml
  - service.yaml

configMapGenerator:
  - name: {{APP_NAME}}
    literals: []
